# Phase Gate: phase_foundation

**Written before build begins. Criteria here cannot be loosened mid-phase.**

This is phase 1 of 13. There is no prior phase to improve over; the "Must Improve" section establishes absolute baselines that every subsequent phase inherits and must not regress.

---

## Must Pass (BLOCKERS — any failure rejects the gate)

### Monorepo & Tooling

- [ ] `pnpm install` resolves from a clean node_modules with zero warnings
  - **Verification:** `rm -rf node_modules && pnpm install 2>&1 | grep -c "WARN"` returns 0
- [ ] `pnpm -r typecheck` exits 0 across all packages and apps in strict mode
  - **Verification:** `pnpm -r typecheck` exits 0; `tsconfig.json` in every package includes `"strict": true`
- [ ] `pnpm -r lint` exits 0 (ESLint + Prettier)
  - **Verification:** `pnpm -r lint` exits 0
- [ ] `pnpm -r build` exits 0, producing deployable artifacts for web, api, and voice
  - **Verification:** `pnpm -r build` exits 0; `apps/web/.next`, `apps/api/dist`, `apps/voice/dist` directories exist
- [ ] Turborepo caching is operative: second cold build (after `--force`) completes in ≤2s for a no-change run
  - **Verification:** `pnpm build --force && pnpm build` — second run stdout contains "FULL TURBO" or reports 0 tasks to execute
- [ ] Pre-commit hook blocks a commit containing a lint/typecheck violation
  - **Verification:** Introduce a deliberate type error, attempt `git commit`; hook exits non-zero and aborts the commit

### Database

- [ ] `pnpm db:migrate` applies the `health_checks` migration against a fresh Postgres 16 instance with no errors
  - **Verification:** `pnpm db:migrate` exits 0; `psql -c "\d health_checks"` shows columns `id`, `service`, `status`, `checked_at`
- [ ] `pnpm db:migrate:down` reverts the migration cleanly; table no longer exists
  - **Verification:** `pnpm db:migrate:down` exits 0; `psql -c "\d health_checks"` returns "did not find any relation"
- [ ] Migrations are stored as SQL files under `packages/db/migrations/`, not generated by `drizzle-kit push`
  - **Verification:** `ls packages/db/migrations/*.sql | wc -l` ≥ 1; no `drizzle-kit push` call appears in CI or scripts
- [ ] Integration test: writes a `health_checks` row and reads it back via the Drizzle ORM client
  - **Verification:** `pnpm --filter db test` exits 0

### API Service (`apps/api`)

- [ ] `pnpm --filter api dev` boots on port 3001 within 10s
  - **Verification:** `curl -s http://localhost:3001/healthz | jq .ok` returns `true`
- [ ] `GET /healthz` returns HTTP 200 `{ok:true, db:"up", redis:"up"}` when both are reachable; returns HTTP 503 when either is unreachable
  - **Verification:** Integration test for the 200 and 503 cases both pass (`pnpm --filter api test`)
- [ ] Fastify plugins present: `@fastify/sensible`, `@fastify/helmet`, `@fastify/cors`, `@fastify/rate-limit`, `@fastify/compress`
  - **Verification:** `grep -r "fastify/sensible\|fastify/helmet\|fastify/cors\|fastify/rate-limit\|fastify/compress" apps/api/src` returns matches for each
- [ ] Logs are structured JSON; every request log includes a request ID field
  - **Verification:** `curl http://localhost:3001/healthz` emits a JSON log line containing `"reqId"` (or equivalent pino field)
- [ ] Graceful shutdown: on SIGTERM the process drains in-flight requests and closes the DB pool before exiting
  - **Verification:** Integration test sends SIGTERM while a slow request is in-flight; process exits 0 after request completes

### Web App (`apps/web`)

- [ ] `pnpm --filter web dev` boots on port 3000 within 15s
  - **Verification:** `curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/` returns `200`
- [ ] Homepage renders "Service.AI" text and issues a network request to `GET /api/v1/health` (or `/healthz` forwarded via Next.js rewrite)
  - **Verification:** Browser DevTools (or Playwright trace) shows the outbound fetch; response is `{ok:true}`
- [ ] Tailwind styles apply; shadcn/ui is importable without errors
  - **Verification:** `pnpm --filter web build` exits 0; no "Cannot find module" errors related to shadcn
- [ ] `pnpm --filter web build` succeeds and produces a production bundle
  - **Verification:** `apps/web/.next/standalone` or `apps/web/.next` directory exists after build

### Voice Service (`apps/voice`)

- [ ] `pnpm --filter voice dev` boots on port 8080 within 10s
  - **Verification:** `curl -s http://localhost:8080/healthz | jq .ok` returns `true`
- [ ] WebSocket endpoint at `ws://localhost:8080/call` completes handshake successfully
  - **Verification:** `wscat -c ws://localhost:8080/call` connects without error
- [ ] Echo test: client sends `"ping"` and receives `"pong"` within 50ms
  - **Verification:** `pnpm --filter voice test` includes an echo latency assertion that passes

### ts-rest Contracts

- [ ] `packages/contracts/src/echo.ts` exists and defines the `/api/v1/echo` contract with Zod input/output schemas
  - **Verification:** `cat packages/contracts/src/echo.ts` contains both a Zod input schema and a Zod response schema
- [ ] API implements the echo contract; `POST /api/v1/echo` returns `{ok:true, data:{echo:<input>}}`
  - **Verification:** `curl -X POST http://localhost:3001/api/v1/echo -H "Content-Type: application/json" -d '{"message":"hello"}' | jq .data.echo` returns `"hello"`
- [ ] Web uses the ts-rest typed client; a type error in the response shape causes a TypeScript compile error
  - **Verification:** Introduce a deliberate shape mismatch in the client consumer; `pnpm -r typecheck` exits non-zero
- [ ] Integration tests: happy-path roundtrip + 400 on invalid input — both pass
  - **Verification:** `pnpm --filter api test` includes both cases and exits 0

### CI/CD

- [ ] `.github/workflows/ci.yml` exists with jobs: typecheck, lint, test, build; triggers on push to any branch and on pull_request
  - **Verification:** `cat .github/workflows/ci.yml` contains `on: [push, pull_request]` and all four job steps
- [ ] All CI checks pass on a clean clone of `main`
  - **Verification:** Latest GitHub Actions run on `main` branch shows all jobs green
- [ ] pnpm store caching reduces CI second run to <3 min total
  - **Verification:** CI timing in the Actions run shows ≤3 min wall time on a cached run
- [ ] A deliberate test failure causes the CI workflow to fail and exit non-zero
  - **Verification:** Documented in CI configuration; a failing test branch shows the check as failed

### Observability

- [ ] A log line emitted by `apps/api` (via pino + `@axiomhq/pino`) appears in Axiom within 10s of the request
  - **Verification:** After hitting `/healthz` on the staging deployment, the Axiom dataset shows the log within 10s
- [ ] An uncaught error thrown in `apps/api` creates a Sentry event with request context (URL, method, request ID)
  - **Verification:** Trigger the test error route; Sentry event appears in the project within 30s
- [ ] Web client-side errors (e.g., thrown in a React event handler) report to Sentry
  - **Verification:** Sentry web project receives at least one test event; event includes `user.agent`
- [ ] Secrets (Authorization header, cookie values, tokens) are redacted in logs
  - **Verification:** Log output for a request containing an `Authorization` header shows `[REDACTED]` or similar; not the raw value
- [ ] Axiom + Sentry are disabled when their env vars are unset (local dev default)
  - **Verification:** `unset AXIOM_TOKEN SENTRY_DSN && pnpm --filter api dev` starts without errors or connection warnings

### DigitalOcean App Platform

- [ ] `.do/app.yaml` defines three components (web, api, voice) with environment variable references, managed Postgres, and managed Redis
  - **Verification:** `doctl apps spec validate .do/app.yaml` exits 0
- [ ] First deployment to the DO staging app creates all three services and both managed databases
  - **Verification:** `doctl apps list` shows the app; `doctl apps get <app-id>` shows all components `ACTIVE`
- [ ] `GET /healthz` on the deployed staging API returns HTTP 200 `{ok:true, db:"up", redis:"up"}`
  - **Verification:** `curl https://<staging-api-url>/healthz | jq .ok` returns `true`
- [ ] Push to `main` triggers an automatic redeploy that completes within 5 min
  - **Verification:** After a dummy commit+push, DO App Platform shows a new deployment completing; confirmed in DO console or `doctl apps list-deployments <app-id>`
- [ ] Rollback procedure is documented in `README.md`
  - **Verification:** `grep -i "rollback" README.md` returns at least one meaningful paragraph describing the procedure

### Docker Compose (Local Dev)

- [ ] `docker compose up` brings all 5 containers (web, api, voice, postgres, redis) to healthy status within 60s
  - **Verification:** `docker compose up -d && sleep 60 && docker compose ps` shows all containers as "healthy" or "running"
- [ ] All services reach each other by Docker service name (e.g., api → postgres, api → redis)
  - **Verification:** `docker compose exec api curl -s http://voice:8080/healthz | jq .ok` returns `true`
- [ ] Port mapping: web 3000, api 3001, voice 8080, postgres 5434, redis 6381
  - **Verification:** `docker compose port web 3000`, `docker compose port api 3001`, etc. return the mapped host ports
- [ ] Editing a source file triggers hot reload in the correct service within 5s
  - **Verification:** Modify `apps/api/src/index.ts`, observe restart log line within 5s in `docker compose logs api`

### Test Coverage

- [ ] Unit + integration test suite passes with zero failures: `pnpm -r test` exits 0
  - **Verification:** `pnpm -r test` exits 0; no skipped tests on code paths (only infrastructure-dependent tests may be conditionally skipped with a documented reason)
- [ ] Code coverage on foundation packages (`packages/db`, `packages/contracts`) and foundation routes (`/healthz`, `/echo`) ≥ 80%
  - **Verification:** `pnpm -r coverage` (or `vitest run --coverage`) shows ≥ 80% line coverage for the named paths
- [ ] Zero BLOCKER findings in the final audit
  - **Verification:** Latest `phases/phase_foundation_AUDIT_*.md` shows `BLOCKERS: 0`

### Security Baseline

- [ ] No secrets committed to the repository
  - **Verification:** `git log --all --full-history -p | grep -E "(password|secret|token|api_key)" | grep -v "REDACTED\|placeholder\|example\|<"` returns no real secret values; `.env` is in `.gitignore`
- [ ] `pnpm audit --audit-level=high` reports zero high or critical vulnerabilities
  - **Verification:** `pnpm audit --audit-level=high` exits 0

---

## Must Improve Over Previous Phase

_(Phase 1 of 13 — no prior phase exists. The items below establish absolute baselines that every subsequent gate must not regress.)_

- [ ] **Baseline: CI wall time ≤ 5 min on a warm cache**
  - **Verification:** CI timing on the most recent `main` run is ≤ 5 min; recorded in this file once gate passes
- [ ] **Baseline: `pnpm -r build` wall time ≤ 60s on developer hardware (M-series Mac or equivalent)**
  - **Verification:** `time pnpm -r build` output recorded; subsequent phases track delta
- [ ] **Baseline: web production bundle size** (Next.js first-load JS)
  - **Verification:** `pnpm --filter web build` output shows First Load JS; value recorded here for future gate comparisons
- [ ] **Baseline: zero `pnpm audit` high/critical findings**
  - **Verification:** `pnpm audit --audit-level=high` exits 0; count = 0

---

## Documentation

- [ ] `README.md` exists at repo root with: prerequisites, `docker compose up` quick-start, per-service dev commands, environment variable reference, rollback procedure, and how to run tests
  - **Verification:** `cat README.md` contains sections for each of the above; a new developer can follow it cold
- [ ] `docs/ARCHITECTURE.md` documents the three-service topology, package dependency graph, and the local vs. DO environment parity strategy
  - **Verification:** `cat docs/ARCHITECTURE.md` covers all three points

---

## Gate Decision

_(Filled in by reviewer after all BLOCKER criteria are verified)_

**Verdict:** APPROVED

**Reviewer:** Gate Reviewer Agent
**Date:** 2026-04-21
**Notes:** All mechanically verifiable blocker criteria independently confirmed at commit 83ae9a3d697464af7929fa12652b4d8ff8fd3099. `pnpm -r build` exits 0; all three artifact directories exist (`apps/web/.next/`, `apps/api/dist/`, `apps/voice/dist/`). `pnpm -r typecheck` exits 0 across all packages in strict mode. `pnpm -r test` exits 0 with 118 tests passing (contracts: 20, db: 19, api: 48, voice: 11, web: 20). `pnpm audit --audit-level=high` exits 0 with zero high/critical findings (3 moderate only). `page.tsx` correctly uses ts-rest typed client calling `apiClient.echo()` and accessing `result.body.data.echo`. `packages/db/vitest.config.ts` excludes `dist/**`. SQL migration files exist (`0001_health_checks.sql`, `0001_health_checks.down.sql`). CI workflow has all four jobs (typecheck, lint, test, build) with correct triggers (`push: branches: ['**']` and `pull_request`). Rollup override `>=3.30.0` confirmed in root `package.json`. Six non-blocking warnings from Audit 4 are carried forward: W1 (web test passes on comment text rather than behavior), W2 (duplicate comment block in app.ts), W3 (missing Sentry global-error.js handler), W4 (no healthcheck stanzas on app containers in Docker Compose), W5 (Next.js ESLint plugin not wired), W6 (ARCHITECTURE.md lacks explicit dependency graph). None of these rise to blocker level. Phase foundation is complete.
