{
  "name": "@service-ai/contracts",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Service.AI shared contracts — Zod schemas and ts-rest route definitions",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vitest run",
    "coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@ts-rest/core": "^3",
    "zod": "^3"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.0.0",
    "typescript": "^5",
    "vitest": "^3"
  }
}
