{
  "name": "@service-ai/auth",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Service.AI auth package — Better Auth config and middleware",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./sender": "./src/sender.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vitest run",
    "coverage": "vitest run --coverage"
  },
  "dependencies": {
    "better-auth": "^1.1.0"
  },
  "devDependencies": {
    "@types/node": "^22",
    "@vitest/coverage-v8": "^3",
    "fastify": "^5",
    "typescript": "^5.4.0",
    "vitest": "^3"
  },
  "peerDependencies": {
    "fastify": "^5"
  }
}
