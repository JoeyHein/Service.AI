{
  "name": "@service-ai/ai",
  "version": "0.0.1",
  "private": true,
  "description": "Service.AI AI router — multi-provider LLM routing, prompt library, RAG client",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "test": "echo 'No tests in stub package'"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
