{
  "name": "@service-ai/db",
  "version": "0.0.1",
  "private": true,
  "description": "Service.AI database package — Drizzle ORM schema and migrations",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vitest run"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
